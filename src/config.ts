// Flip to true once all Qortium apps are deployed and cross-app deep links are verified.
// Search for ENABLE_CROSS_APP_LINKS across the repo to find every flag at once.
export const ENABLE_CROSS_APP_LINKS = true;
